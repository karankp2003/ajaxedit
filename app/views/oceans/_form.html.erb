<%= form_for(@ocean) do |f| %>
  <% if @ocean.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ocean.errors.count, "error") %> prohibited this ocean from being saved:</h2>

      <ul>
      <% @ocean.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
